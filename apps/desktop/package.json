{
  "name": "@openvisionmatrix/desktop",
  "version": "0.0.0",
  "private": true,
  "author": "Oliver Welter <oliver@welter.rocks>",
  "description": "OpenVisionMatrix Desktop",
  "main": "dist/main.js",
  "dependencies": {
    "7zip-bin": "^5.2.0"
  },
  "scripts": {
    "build:main": "tsc -p tsconfig.json",
    "build:renderer": "node scripts/build-renderer.mjs",
    "build": "npm run build:main && npm run build:renderer && electron-builder --config electron-builder.json",
    "dev": "node scripts/dev.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.14.2",
    "electron": "39.2.7",
    "electron-builder": "^24.13.3",
    "typescript": "^5.5.4"
  }
}
